# Copy this file to .env.local and keep .env.local out of version control (it already is, see .gitignore)
NEXT_PUBLIC_SANITY_PROJECT_ID=k5fw7bl7
NEXT_PUBLIC_SANITY_DATASET=production

# Needed only for the /join page's submission form (server-side only, never
# exposed to the browser). Create one at sanity.io/manage → your project →
# API → Tokens → Add API token → permission: Editor (or Create-only).
SANITY_API_TOKEN=
